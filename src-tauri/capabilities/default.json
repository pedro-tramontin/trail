{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main + onboarding windows",
  "windows": ["main", "onboarding"],
  "permissions": [
    "core:default"
  ]
}
